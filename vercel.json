{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci --include=dev --include=optional --workspaces --include-workspace-root",
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/.next",
  "regions": [
    "sin1"
  ],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
